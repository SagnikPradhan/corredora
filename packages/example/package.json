{
  "name": "example",
  "scripts": {
    "run": "ts-node --project ./tsconfig.json ./source/index.ts",
    "dev": "nodemon -x \"yarn concurrently yarn:service yarn:run\"",
    "service": "corredora"
  },
  "dependencies": {
    "@corredora/cli": "workspace:packages/cli",
    "@corredora/logger": "workspace:packages/library"
  },
  "devDependencies": {
    "@types/concurrently": "^6",
    "concurrently": "^6.1.0",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
